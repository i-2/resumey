import React from 'react';
import {
    Flex, FormControl, FormLabel, Textarea, FormErrorMessage, Button
} from '@chakra-ui/react';
import {
    Field,
    Formik,
    Form,
    FieldArray
} from 'formik';
import * as Yup from 'yup';

export const EmploymentHistory = (props: any) => 
    (
        <div>

        </div>
    )